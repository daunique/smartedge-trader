# Trimmed to what backend/app actually imports. Removed (were never used
# anywhere in the codebase, confirmed by grep before deleting): scikit-learn,
# xgboost (the ML layer, per removal request), ccxt (only used by the
# orphaned exchange/bybit.py, which called an unused ccxt connector while
# every real request goes through direct signed HTTP calls -- both removed),
# redis, sqlalchemy, alembic, asyncpg, python-jose, passlib, schedule,
# pydantic, pydantic-settings, pandas. Fewer unused deps = faster, less
# fragile installs on Render.
fastapi==0.110.0
uvicorn[standard]==0.27.1
websockets==12.0
python-dotenv==1.0.1
numpy==1.26.4
httpx==0.26.0
